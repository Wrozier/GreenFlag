# GreenFlag
MVP-first approach with Jetpack Compose
The requirements from the provided screens/PDF: welcome promo → registration form with strict password rules → enable Next only when valid
Added real-time validation and error UI.
Throughout I focused on maintainability, reusability, and following current Compose patterns
